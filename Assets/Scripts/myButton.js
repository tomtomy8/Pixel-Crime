var player : Brawler;

var mainCamera : Camera;

/*function Update() {
	var hit : RaycastHit;
	if(gameObject.GetComponent(Collider).Raycast(mainCamera.ScreenPointToRay(Input.mousePosition), hit, 1f)) {
		Debug.Log("Clicked");
	} 
}*/	